import { defineConfig } from 'astro/config';

export default defineConfig({
  outDir: 'docs', // Change output directory to /docs
  site: 'https://davinceleecode.github.io/', // Replace with your GitHub Pages URL
});
